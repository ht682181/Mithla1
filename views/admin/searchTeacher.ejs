 <% layout("/layouts/boilerplate") -%>
<div class="container my-3">

  
 
  <div class="card shadow-lg border-0 rounded-4">
    <div class="card-header bg-dark text-white rounded-top-4">
      <h4 class="mb-0 fw-bold text-center">📋 Teacher Details</h4>
    </div>
    <div class="card-body">
      <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle text-center shadow-sm">
          <thead class="table-primary">
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Mobile</th>
              <th>Edit</th>
              <th>Delete</th>
             
               <th>Subject&Class</th>
            
            </tr>
          </thead>
          <tbody>
            <% for (let data of datas) { %>
              <tr>
                 <td><a href="/teacher/profile/<%= data._id %>" style="color: black; text-decoration: none;"><%= data.name %></a></td>
                <td><%= data.email %></td>
                <td><%= data.mobile %></td>
                <td>
                  <a href="/edit/teacher/<%= data._id %>" class="btn btn-sm btn-outline-warning"><i class="fa-solid fa-pen"></i></a>
                </td>
                <td>
                  <form action="/delete/teacher/<%= data._id %>?_method=Delete" method="post" style="display:inline;">
                    <button class="btn btn-sm btn-outline-danger"><i class="fa-solid fa-trash"></i></button>
                  </form>
                </td>
                
                 <td>
                 <form action="/show/teacher/class/<%= data._id %>" method="get">
                    <button class="btn btn-sm btn-outline-secondary">View</button>
                  </form>
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<style>
  
  
  .bg-light-glass {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(8px);
  }
  .btn-gradient {
    background: linear-gradient(45deg, #0d6efd, #6610f2);
    color: #fff;
    border: none;
  }
  .btn-gradient:hover {
    opacity: 0.9;
    color: #fff;
  }
</style>
