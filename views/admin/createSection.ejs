<% layout("/layouts/boilerplate") -%>

<div class="container my-5">

  <!-- Show Section Button -->
  <div class="text-center mb-4">
    <a href="/show/section"
      class="btn btn-dark shadow-sm px-4 py-2 rounded-pill fw-semibold d-inline-flex align-items-center gap-2 fs-6">
      <i class="fa-solid fa-eye"></i> Show Sections
    </a>
  </div>

  <!-- Form Card -->
  <div class="row justify-content-center">
    <!-- ⬇️ column bada -->
    <div class="col-md-7 col-lg-6">

      <div class="card shadow-lg border-0 rounded-4">
        <!-- ⬇️ padding increase -->
        <div class="card-body p-5">

          <!-- ⬇️ heading thodi badi -->
          <h3 class="fw-bold text-center mb-4 text-primary">
            🔱 Add Section
          </h3>

          <form action="/add/section" method="POST" novalidate>

            <div class="mb-4">
              <label class="form-label fw-semibold fs-5">
                Section Name
              </label>

              <!-- 🔥 MAIN BOX BADA -->
              <input
                type="text"
                name="data[name]"
                class="form-control rounded-3 shadow-sm fs-3 fw-bold text-center section-input"
                placeholder="A"
                maxlength="1"
                required
              />

              <small class="text-muted fs-6">
                Only one letter (A–Z) is allowed
              </small>
            </div>

            <div class="d-grid mt-4">
              <button type="submit"
                class="btn btn-primary btn-lg rounded-pill shadow py-3 fs-5">
                💾 Save Section
              </button>
            </div>

          </form>

        </div>
      </div>

    </div>
  </div>
</div>

<style>
  /* 🔥 Input box ki real height */
  .section-input{
    height: 80px;
    letter-spacing: 8px;
  }

  .form-control:focus {
    border-color: #0d6efd;
    box-shadow: 0 0 0 0.25rem rgba(13,110,253,.35);
  }
</style>
